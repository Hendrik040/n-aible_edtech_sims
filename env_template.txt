# Copy this to .env and fill in your values

# Database Configuration - PostgreSQL (primary database)
# For development: postgresql://username:password@localhost:5432/ai_agent_platform
# For production: postgresql://username:password@hostname:5432/ai_agent_platform?sslmode=require
# For SQLite (development only): sqlite:///./ai_agent_platform.db
DATABASE_URL=REPLACE_WITH_YOUR_DATABASE_URL

# AI Service API Keys - Replace with your actual API keys
# These are dummy values - you must provide real API keys from OpenAI and Anthropic
OPENAI_API_KEY=REPLACE_WITH_YOUR_OPENAI_API_KEY_HERE

# Redis Configuration (REQUIRED)
# Redis is required for session management, caching, and performance optimization
# For development
# REDIS_URL=redis://localhost:6379
REDIS_URL=REPLACE_WITH_YOUR_REDIS_URL

# Security - Change this to a strong random key
# This is a dummy value - generate a proper random secret and never commit it to version control (python3 -c "import secrets; print(secrets.token_urlsafe(64))")
SECRET_KEY=CHANGE_ME_GENERATE_RANDOM_SECRET_KEY

# Environment
ENVIRONMENT=REPLACE_WITH_YOUR_ENVIRONMENT

# CORS Origins (comma-separated) - Add your frontend URLs
# For development: 
# CORS_ORIGINS=http://localhost:3000,http://localhost:3001
CORS_ORIGINS=REPLACE_WITH_YOUR_CORS_ORIGINS

# Google OAuth Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com/
# These are dummy values - you must provide real OAuth credentials from Google Cloud Console
GOOGLE_CLIENT_ID=REPLACE_WITH_YOUR_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=REPLACE_WITH_YOUR_GOOGLE_CLIENT_SECRET
GOOGLE_REDIRECT_URI=REPLACE_WITH_YOUR_GOOGLE_REDIRECT_URI

# LlamaParse Configuration
LLAMAPARSE_API_KEY=REPLACE_WITH_YOUR_LLAMAPARSE_API_KEY

# Frontend Configuration
# For development
# FRONTEND_BASE_URL=http://localhost:3000
# FRONTEND_URL=http://localhost:3000
FRONTEND_BASE_URL=REPLACE_WITH_YOUR_FRONTEND_BASE_URL
FRONTEND_URL=REPLACE_WITH_YOUR_FRONTEND_URL

# Backend Configuration
# This is used for generating full URLs for static files (e.g., DALL-E images)
# In development: http://localhost:8001
# In production: https://your-backend-domain.railway.app
BACKEND_URL=http://localhost:8001

# Cookie Configuration (for cross-origin authentication)
# Leave empty for auto-detection, or set to your domain (e.g., .railway.app)
COOKIE_DOMAIN=
COOKIE_SECURE=false
